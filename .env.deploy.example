# Cloudflare Workers deploy vars
# VARS are plain_text bindings (visible in dashboard); secrets are set via API.
VARS=SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY
SUPABASE_URL=https://fphchylxhcghlvfkvkfz.supabase.co
SUPABASE_PUBLISHABLE_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
GOOGLE_SERVICE_ACCOUNT_JSON={"client_email":"gbpims@gbpims-507606.iam.gserviceaccount.com","private_key":"-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n"}
# Optional short-URL tuning (set as env when running deploy script or GitHub secrets):
# WORKER_NAME=gbpims          # default short name → https://gbpims.<subdomain>.workers.dev (use "gbp" for even shorter)
# CLOUDFLARE_CUSTOM_DOMAIN=gbpims.example.com  # bind custom domain for tiny URL (domain must be on Cloudflare)
